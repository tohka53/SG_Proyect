@model IEnumerable<SG_Proyect.Models.Shopping_Cart>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.monto_total)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.nombre_cliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado1.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Metodo_Pago.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.monto_total)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.nombre_cliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado1.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Metodo_Pago.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_shoppingcart }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_shoppingcart }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_shoppingcart })
        </td>
    </tr>
}

</table>
